@file:Suppress("DEPRECATION")

// Generated by protokt version 0.12.1. Do not modify.
// Source: google/type/calendar_period.proto
package com.google.type

import com.toasttab.protokt.rt.KtEnum
import com.toasttab.protokt.rt.KtEnumDeserializer
import kotlin.Int
import kotlin.String

@Deprecated("use v1")
sealed class CalendarPeriod(
    override val `value`: Int,
    override val name: String,
) : KtEnum() {
    object CALENDAR_PERIOD_UNSPECIFIED : CalendarPeriod(0, "CALENDAR_PERIOD_UNSPECIFIED")

    object DAY : CalendarPeriod(1, "DAY")

    object WEEK : CalendarPeriod(2, "WEEK")

    object FORTNIGHT : CalendarPeriod(3, "FORTNIGHT")

    object MONTH : CalendarPeriod(4, "MONTH")

    object QUARTER : CalendarPeriod(5, "QUARTER")

    object HALF : CalendarPeriod(6, "HALF")

    object YEAR : CalendarPeriod(7, "YEAR")

    class UNRECOGNIZED(
        `value`: Int,
    ) : CalendarPeriod(value, "UNRECOGNIZED")

    companion object Deserializer : KtEnumDeserializer<CalendarPeriod> {
        override fun from(`value`: Int): CalendarPeriod = when (value) {
          0 -> CALENDAR_PERIOD_UNSPECIFIED
          1 -> DAY
          2 -> WEEK
          3 -> FORTNIGHT
          4 -> MONTH
          5 -> QUARTER
          6 -> HALF
          7 -> YEAR
          else -> UNRECOGNIZED(value)
        }
    }
}
